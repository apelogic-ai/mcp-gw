export interface ProjectMetadata {
  name: "mcp-gateway";
  stack: "bun-typescript";
  agentAgnostic: true;
}

export const projectMetadata: ProjectMetadata = {
  name: "mcp-gateway",
  stack: "bun-typescript",
  agentAgnostic: true,
};
