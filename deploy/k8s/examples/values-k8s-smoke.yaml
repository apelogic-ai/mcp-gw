hop1:
  issuers:
    - name: unavailable
      issuer: https://unavailable.example.com
      audiences:
        - https://mcp.example.com/mcp
      jwksUrl: http://127.0.0.1:9/.well-known/jwks.json
      discoverable: true
      emailClaim: email
      subjectClaim: sub

agentgateway:
  mcpAuthentication:
    resourceMetadata:
      resource: https://mcp.example.com/mcp
      scopesSupported:
        - openid
        - email
      bearerMethodsSupported:
        - header
