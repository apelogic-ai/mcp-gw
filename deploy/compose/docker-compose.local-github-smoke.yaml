services:
  agentgateway:
    volumes:
      - ../../gateway/agentgateway/local-github-smoke.yaml:/etc/agentgateway/config.yaml:ro

  github-wrapper:
    extra_hosts:
      - host.docker.internal:host-gateway
    environment:
      HOP1_ISSUERS_JSON: ${GITHUB_SMOKE_HOP1_ISSUERS_JSON}
